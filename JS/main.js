// document.querySelector('.pesquisa-card input').addEventListener('keypress', function(event) {
//     if (event.key === 'Enter') {
//         const termoPesquisa = event.target.value;
//         window.location.href = `./HTML/pesquisa.html?query=${encodeURIComponent(termoPesquisa)}`;
//     }
// });
