// document.querySelector('.pesquisa-card input').addEventListener('keypress', function (event) {
//     if (event.key === 'Enter') {
//         const query = event.target.value.toLowerCase();

//         // Redireciona para a página de pesquisa e passa a consulta na URL
//         window.location.href = `pesquisa.html?q=${encodeURIComponent(query)}`;
//     }
// });


